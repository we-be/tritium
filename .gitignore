# Config with real addresses; only .env.example is tracked
.env
*.env

# Build and cluster output
bin/
logs/
*.log
*.pid
*.test
*.out
go.work

# examples/go builds to a binary named "go" (the directory's basename)
# unless -o is given
examples/go/go
__pycache__/
